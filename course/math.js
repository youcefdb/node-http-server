const add = (a, b) => (a + b);
const sub = (a, b) => (a - b);
const div = (a, b) => (a / b);
const mul = (a, b) => (a * b);


export {mul};

//This is a module, we it's like a class, but it can hold multiple funs and vars, export for exporting